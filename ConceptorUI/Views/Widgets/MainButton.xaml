<UserControl x:Class="ConceptorUI.Views.Widgets.MainButton"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d"
             d:DesignHeight="300"
             d:DesignWidth="300">

    <StackPanel Width="40">
        <Button Tag="Save"
                Width="30"
                Height="30"
                Cursor="Hand"
                Click="OnClick"
                Background="#e1e1e1"
                BorderBrush="{Binding BorderBrush, RelativeSource={RelativeSource AncestorType=UserControl}}"
                BorderThickness="0.4"
                VerticalAlignment="Top"
                HorizontalAlignment="Center">
            <materialDesign:PackIcon Kind="{Binding Kind, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                     Width="22"
                                     Height="22"
                                     Foreground="{Binding BorderBrush, RelativeSource={RelativeSource AncestorType=UserControl}}" />
        </Button>
        <TextBlock Text="{Binding Content, RelativeSource={RelativeSource AncestorType=UserControl}}"
                   FontSize="6"
                   HorizontalAlignment="Center"
                   Foreground="#000000" />
    </StackPanel>
</UserControl>